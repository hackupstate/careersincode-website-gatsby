# Redirect the PDFS in /assets/files to the root level /
# Note that the assets are contained in /static but build to /
/assets/files/* /:splat 301!

# Redirect default Netlify subdomain to primary domain
http://careersincode.netlify.app/* http://careersincode.org/:splat 301!
https://careersincode.netlify.app/* https://careersincode.org/:splat 301!